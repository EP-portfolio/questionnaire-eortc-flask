# Dépendances Flask essentielles
Flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0

# Base de données
# SQLite est intégré à Python, pas besoin de dépendance

# Requêtes HTTP
requests==2.31.0

# Optionnel : pour les variables d'environnement
python-dotenv==1.0.0

# Reconnaissance vocale (pour Firefox/Safari - écoute continue)
faster-whisper
# Dépendances faster-whisper (installées automatiquement)
# - torch (PyTorch optimisé)
# - ffmpeg-python
# - numpy
# - tiktoken