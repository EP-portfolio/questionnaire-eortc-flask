# Dépendances Flask essentielles
Flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0

# Base de données
# SQLite est intégré à Python, pas besoin de dépendance

# Requêtes HTTP
requests==2.31.0

# Optionnel : pour les variables d'environnement
python-dotenv==1.0.0

# Reconnaissance vocale (pour Firefox/Safari - écoute continue)
# Utilisation de Google Cloud Speech-to-Text (gratuit 60min/mois, pas de dépendances système)
# faster-whisper nécessite FFmpeg système non disponible sur Render